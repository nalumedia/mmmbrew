<%= form_for(@brewlink) do |f| %>
  <% if @brewlink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brewlink.errors.count, "error") %> prohibited this brewlink from being saved:</h2>

      <ul>
      <% @brewlink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brewlink_name %><br>
    <%= f.text_field :brewlink_name %>
  </div>
  <div class="field">
    <%= f.label :brewlink_description %><br>
    <%= f.text_area :brewlink_description %>
  </div>
  <div class="field">
    <%= f.label :brewlink_url %><br>
    <%= f.text_field :brewlink_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
